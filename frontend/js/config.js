// Configuration globale du front.
// En local : http://localhost:8080 — en prod : URL Railway de l'API.
window.APP_CONFIG = {
  // Adapter cette URL selon l'environnement (sans slash final)
  API_BASE_URL: "http://localhost:8080",
};
